<!DOCTYPE html>
<html lang="en">
    <%- include("partials/head") %>
<body>
    <%- include("partials/nav") %>

    <h1> All Authors </h1>

     <% for (let i of authors) { %>
 
      <a href="/updateAuthor?authorId=<%= i.authorId %>">Edit</a>
      <a href="/deleteAuthor?authorId=<%= i.authorId %>">Delete</a>

       <%= i.firstName %> <%= i.lastName %> <br>
    <% } %>
</body>
</html>